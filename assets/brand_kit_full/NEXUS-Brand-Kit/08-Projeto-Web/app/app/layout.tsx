import type {Metadata} from 'next';
export const metadata:Metadata={title:'NEXUS — Jogos, guia e minha conta',description:'Sua agenda de esportes, favoritos, canais e assinatura NEXUS PlayTV.'};
export default function ClientLayout({children}:{children:React.ReactNode}){return children}
